name: Artemis 6-Hour Sync
on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch: # Keeps the manual button
  push:
    branches:
      - main  # AUTOMATIC: Runs whenever you commit to the main branch
    paths:
      - 'artemis-1.js' # OPTIONAL: Only run if this specific file changes
